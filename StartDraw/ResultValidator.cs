using System.Collections.Generic;

namespace StartDraw {
  public class ResultValidator {
    public bool Valid(List<StartListEntry> entries, List<StartListEntry> result) {
      /*
      //validate
      number of entries the same.
      each person only shows once.
      number of different persons from same country in each heat should not be more than one.
      no same start time for a heat
      AvoidConsecutiveTimesFromSameNation
      StartGroupRequests      
      SpreadingSimilarRankedRunnersAcrossHeats
      number of people in each heat should be no more than one difference.
      */

      return true;
    }
  }
}